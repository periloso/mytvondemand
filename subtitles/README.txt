The file must contain the function "fetchSubtitles".

These are the parameters: ($showtitle, $season, $episode, $language)

The function must return the subtitle as a string.



After adding the script to the directory subtitles/, the
script will be automatically listed into the configuration.